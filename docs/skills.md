# Skills

Enabled skills live under:

```text
~/.poke/skills/enabled
```

Disabled skills live under:

```text
~/.poke/skills/disabled
```

Each skill has a `SKILL.md` with `name` and `description` frontmatter. Poke seeds `skill-creator` and `poke` during setup.
